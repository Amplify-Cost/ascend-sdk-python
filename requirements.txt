# Core FastAPI dependencies
fastapi==0.104.1
uvicorn[standard]==0.24.0
sqlalchemy==2.0.23
pydantic[email]==2.5.0
python-dotenv==1.0.0
psycopg2-binary==2.9.9
python-multipart

# AI/LLM integration
openai==1.3.7
httpx>=0.25.2,<1.0

# API utilities and security
slowapi==0.1.9
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
bleach==6.3.0  # XSS protection via HTML sanitization
requests==2.31.0  # HTTP requests for Cognito JWKS
aiohttp>=3.9.0  # SEC-093: Async HTTP for integration suite

# Enterprise JWT (SEC-081: RS256 unified authentication)
python-jose[cryptography]==3.3.0
PyJWT[crypto]>=2.8.0  # SEC-081: Unified TokenService (RS256 only)
authlib==1.2.1
requests-oauthlib==1.3.1

# SEC-081: Password hashing (Argon2id + pepper)
argon2-cffi>=23.1.0  # SEC-081: OWASP 2024 recommended KDF

# Enterprise Secrets Management
boto3==1.34.0
azure-keyvault-secrets==4.7.0
azure-identity==1.15.0
hvac==2.0.0

# Enterprise Features
redis>=4.5.0
schedule==1.2.0
psutil==5.9.8
APScheduler==3.10.4  # Automated retention cleanup scheduler

# Database migrations
alembic==1.13.1
pydantic-settings>=2.0.0

# Document generation for audit exports
reportlab==4.0.7
PyYAML==6.0.3
pytz==2024.1

# Testing and Coverage (P0.1 - Launch Critical)
pytest>=8.0.0
pytest-cov>=4.1.0
pytest-asyncio>=0.23.0
pytest-httpx>=0.30.0
pytest-timeout>=2.2.0
coverage[toml]>=7.4.0
