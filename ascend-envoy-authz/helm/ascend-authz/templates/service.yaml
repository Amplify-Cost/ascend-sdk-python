apiVersion: v1
kind: Service
metadata:
  name: {{ include "ascend-authz.fullname" . }}
  labels:
    {{- include "ascend-authz.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: grpc
      protocol: TCP
      name: grpc
    - port: {{ .Values.service.metricsPort }}
      targetPort: metrics
      protocol: TCP
      name: metrics
  selector:
    {{- include "ascend-authz.selectorLabels" . | nindent 4 }}
